import { checkStatus } from 'code100days/helpers/check-status';
import { module, test } from 'qunit';

module('Unit | Helper | check status');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = checkStatus([42]);
  assert.ok(result);
});
